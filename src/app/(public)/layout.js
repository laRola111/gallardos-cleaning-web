// src/app/(public)/layout.js
export default function PublicLayout({ children }) {
  // Este layout simplemente renderiza a sus hijos por ahora.
  // Más adelante podría contener elementos comunes solo a las páginas públicas.
  return <>{children}</>;
}